"""
Package list for User components. The UserPackage will be imported into the
configuration parser. All keys in the package ought to reflect the name in the
configuration file and that the value of that entry should be the class name
for the given component. Note that components can mean buses or entities.

As this is the user package, the components added here should follow a similar
form to the predefined components. Additionally, the components should follow
the applicable abstract interfaces.
"""

UserPackage = {
    # Purposefully Empty
}
